name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install
      - run: npm run build
      - uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
```

---

## Étape 4 — Activer GitHub Pages

> **Settings** → **Pages** → Source : **Deploy from a branch** → Branch : **gh-pages** → **Save**

---

## Étape 5 — Attendre le déploiement

Après le premier push, va dans **Actions** pour voir le build tourner (2-3 min). Ton site sera ensuite disponible à :
```
https://qnhihuynh-hebergement.github.io/Trustlynhi/
